import React from "react";
import { StyleSheet, Text, View } from "react-native";

interface Props {}

const EditProductScreen = (props: Props) => {
	return (
		<View>
			<Text>Edit Product scren</Text>
		</View>
	);
};

export default EditProductScreen;

const styles = StyleSheet.create({});
